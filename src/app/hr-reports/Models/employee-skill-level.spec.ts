import { EmployeeSkillLevel } from './employee-skill-level';

describe('EmployeeSkillLevel', () => {
  it('should create an instance', () => {
    expect(new EmployeeSkillLevel()).toBeTruthy();
  });
});
